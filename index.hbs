<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />

    <!-- Always force latest IE rendering engine or request Chrome Frame -->
    <meta content="IE=edge,chrome=1" http-equiv="X-UA-Compatible" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta
      property="og:description"
      content="North by Northwestern's Class of {{year}} seniors reflect on their time at Northwestern and what comes next."
    />
    <meta property="og:title" content="Commencement {{year}} | North by Northwestern" />
    <meta property="og:url" content="{{articleUrl}}" />
    <link rel="shortcut icon" href="https://media.northbynorthwestern.com/media/img/favicon-hi.ico" />

    <title>Commencement {{year}} | North by Northwestern</title>
    <script>
      function equalheight(container) {
        var currentTallest = 0,
          currentRowStart = 0,
          rowDivs = [],
          elem = null,
          topPosition = 0;

        var elemArr = Array(...document.getElementsByClassName("grid-entry"));
        elemArr.forEach((c) => {
          elem = c;
          elem.style.height = "auto";
          topPostion = elem.style.topPosition;

          if (currentRowStart != topPostion) {
            for (currentDiv = 0; currentDiv < rowDivs.length; currentDiv++) {
              rowDivs[currentDiv].height(currentTallest);
            }
            rowDivs.length = 0; // empty the array
            currentRowStart = topPostion;
            currentTallest = elem.style.height;
            rowDivs.push(elem);
          } else {
            rowDivs.push(elem);
            currentTallest = currentTallest < elem.style.height ? elem.style.height : currentTallest;
          }
          for (currentDiv = 0; currentDiv < rowDivs.length; currentDiv++) {
            rowDivs[currentDiv].style.height = currentTallest;
          }
        });
      }

      window.addEventListener("load", (e) => {
        equalheight(".grid-entry");
      });

      window.addEventListener("resize", (e) => {
        equalheight(".grid-entry");
      });
    </script>
  </head>
    <style>
    @font-face {
      font-family: "icomoon";
      src: url("fonts/icomoon-66504f1c.eot?vcxnuv");
      src: url("fonts/icomoon-66504f1c.eot?#iefixvcxnuv") format("embedded-opentype"),
        url("fonts/icomoon-fe508a5c.woff?vcxnuv") format("woff"),
        url("fonts/icomoon-740ddb1c.ttf?vcxnuv") format("truetype"),
        url("fonts/icomoon-7a3d0823.svg?vcxnuv#icomoon") format("svg");
      font-weight: normal;
      font-style: normal;
    }
    [class^="icon-"],
    [class*=" icon-"] {
      font-family: "icomoon";
      speak: none;
      font-style: normal;
      font-weight: normal;
      font-variant: normal;
      text-transform: none;
      line-height: 1;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }
    .icon-point-up:before {
      content: "\ea03";
    }
    .icon-facebook:before {
      content: "\ea8c";
    }
    .icon-twitter:before {
      content: "\ea91";
    }
    .share-icons {
      padding-bottom: 10px;
    }
    .social {
      padding: 0;
      margin: 0;
    }
    .social a {
      color: #501f84;
    }
    .social li {
      display: inline-block;
      width: 30px;
      height: 30px;
    }
    .social li a {
      display: block;
      text-decoration: none;
      font-size: 13px;
    }
    .social li span {
      position: relative;
      top: 10px;
    }
    .social li:hover {
      cursor: pointer;
      color: #2a2a2a;
    }
    .masthead {
      background-color: #501f84;
      padding: 0.5em 0 0.375em;
      text-align: left;
      width: 100%;
    }
    .masthead a {
      color: #501f84;
      text-decoration: none;
      background: transparent;
    }
    .masthead .nbn-logo {
      height: 2em;
    }
    .masthead .logo {
      padding-left: 0.5em;
    }
    .masthead .extra-masthead {
      color: white;
      display: inline-table;
      font-weight: 700;
      height: 100%;
      position: relative;
      top: -0.5em;
      padding-left: 0.5em;
    }
    .masthead .extra-masthead a {
      color: white;
      display: table-cell;
      margin-left: 0.25em;
      text-decoration: none;
      text-transform: uppercase;
    }
    footer {
      font-family: "Montserrat", Helvetica, sans-serif, Helvetica, sans-serif;
      background-color: #501f84;
      color: white;
      margin-top: 50px;
      padding-bottom: 0.5em;
      padding-top: 0.5em;
      width: 100%;
    }
    .credits-container {
      max-width: 68em;
      margin-left: auto;
      margin-right: auto;
      padding: 0 1.25em;
    }
    .credits {
      padding-bottom: 0.5em;
      padding-top: 0.5em;
    }
    .credits p {
      font-size: 0.875em;
      line-height: 1.5em;
      margin: 0;
    }
    .credits p a {
      color: white;
      text-decoration: none;
    }
    *,
    *:before,
    *:after {
      box-sizing: border-box;
    }
    .clearfix:after {
      content: "";
      display: table;
      clear: both;
    }
    body {
      background-color: #1b1b1b;
      margin: 0px;
    }
    .container {
      width: 100%;
      max-width: 990px;
      margin: 0 auto;
      position: relative;
    }
    .extra-masthead {
      font-family: "Montserrat", Helvetica, sans-serif;
      letter-spacing: 1px;
    }
    .circular {
      width: 100%;
    }
    .intro {
      width: inherit;
      text-align: center;
      margin-bottom: 4em;
    }
    .intro a {
      text-decoration: none;
    }
    .intro p {
      font-family: "Alegreya", Georgia, serif;
      font-style: italic;
      color: #fafafa;
      font-weight: 800;
      font-size: 3.25rem;
      padding-bottom: 0.5em;
      padding-top: 50px;
    }
    .intro span {
      background-color: #fafafa;
      color: #501f84;
      text-transform: uppercase;
      font-weight: 600;
      padding: 10px;
      font-family: "Montserrat", Helvetica, sans-serif;
    }
    h1 {
      margin: 0;
      padding-top: 20px;
    }
    p {
      margin: 0;
    }
    .row {
      margin-top: 2em;
    }
    .grid-entry {
      float: left;
      background-color: #fff;
      width: 32%;
      margin-bottom: 2em;
      margin-right: 2%;
    }
    .grid-entry-pos a {
      color: #2a2a2a;
    }
    .grid-entry-pos:last-child .grid-entry {
      margin-right: 0;
    }
    .story-content {
      padding: 30px 20px;
    }
    .story-content .story-title {
      font-family: "Montserrat", Helvetica, sans-serif;
      letter-spacing: 2px;
      font-weight: 400;
      margin-bottom: 5px;
      text-transform: uppercase;
      color: #501f84;
    }
    .story-content .story-author {
      color: #2a2a2a;
      font-family: "Alegreya", Georgia, serif;
      font-style: italic;
      font-size: 2em;
      font-weight: 900;
      letter-spacing: 2px;
    }
    .story-content .story-pullquote {
      margin-top: 10px;
      font-family: "Alegreya", Georgia, serif;
    }
    @media screen and (max-width: 960px) {
      .grid-entry {
        width: 30.67%;
        margin-left: 2%;
        margin-right: 0;
      }
    }
    @media screen and (max-width: 768px) {
      .grid-entry {
        width: 47%;
      }
    }
    @media screen and (max-width: 480px) {
      .grid-entry {
        width: 92%;
        margin-left: 4%;
      }
    }
  </style>

  <body>
    <header class="masthead">
      <div class="logo">
        <a href="https://www.northbynorthwestern.com/">
          <img
            class="nbn-logo"
            src="{{mediaUrlBase}}/nbn-banner-logo.png"
            alt=""
          />
        </a>
        <div class="extra-masthead">
          <a>Commencement {{year}}</a>
          <!-- href="http://apps.northbynorthwestern.com/commencement/{{year}}/" -->
        </div>
      </div>
    </header>
    <div class="intro">
      <a>
        <!-- href="http://apps.northbynorthwestern.com/commencement/{{year}}/" -->
        <p>North by Northwestern</p>
        <span>Commencement {{year}}</span>
      </a>
    </div>

    <div class="container">
        {{#each columns}}
          {{#if (addClearfixDiv @index)}}
            <div class="clearfix">
            {{> columncard}}
          {{else}}
            {{#if (addClearfixDivCloser @index)}}
              {{> columncard}}
              </div>
            {{else}}
              {{> columncard}}
            {{/if}}
          {{/if}}
          {{#if @last}}
            </div>
          {{/if}}
        {{/each}}
    </div>
    <footer>
      <div class="credits-container">
        <div class="credits">
          <p><strong>Published on {{date}}, {{year}}</strong></p>
          <p><strong>Edited by </strong>Justin Curto and Maggie Harden</p>
          <p><strong>Template by </strong>Nicole Zhu</p>
          <p><strong>Modified by </strong>Maxine Whitely</p>
          <p>Ⓒ {{year}} <a href="https://northbynorthwestern.com/" target="_blank">North by Northwestern</a></p>
        </div>
      </div>
    </footer>
  </body>
</html>
